#include <stdio.h>

#include "gui/MainApplication.h"
#include "youtube/CSubs.h"

int main(int argc, char** argv) {
    /*CChannel* c = channel_new_from_name("TomScottGo");

    CVideos vids = channel_get_vids(c);

    printf("Got %d videos\n", vids.length);*/

    return app_run(argc, argv);
}
